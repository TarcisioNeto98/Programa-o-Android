<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">
    <!--o atributo android:button é usado para especificar o caminho do meu layout do
    checkbox(Layout feito como o programador quiser).-->
    <!--O atributo android:button pode ser acessado através dqa interface gráfica(Na paleta ao lado)
    , em button dentro de common attributes, se eu acessar uma imagem png, ao invés de um xml
    a imagem será a mesma tanto para checado quanto para não checado.-->
    <CheckBox
        android:id="@+id/checkBox"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="90dp"
        android:layout_marginTop="121dp"
        android:button="@drawable/checkbox"
        android:text="CheckBox"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />
</androidx.constraintlayout.widget.ConstraintLayout>